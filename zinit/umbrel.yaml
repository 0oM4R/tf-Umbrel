exec: /bin/bash -c "
    echo $NGINX_PORT;
    sudo curl -L https://umbrel.sh | bash -s -- --install-path /test/umbrel --no-install-docker --no-install-compose"
after:
 - dockerd
#  - config
oneshot: true